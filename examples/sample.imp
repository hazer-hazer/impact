type () = builtin "UnitTy"
type i32 = builtin "I32"
type str = builtin "Str"
type Ref = builtin "RefTy"

() = builtin "UnitValue"

(+) = builtin "AddInt"
(-) = builtin "SubInt"

ref = builtin "RefCons"

extern
    puts: str -> i32

data Option a =
    | Some a
    | None

foo () = puts "lol"

main () =
    str = "asd"

    a = ().foo

    ()
