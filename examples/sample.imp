type () = builtin "UnitTy"
type i32 = builtin "I32"
type str = builtin "Str"
type Ref = builtin "RefTy"

() = builtin "UnitValue"

(+) = builtin "AddInt"
(-) = builtin "SubInt"

ref = builtin "RefCons"

extern
    puts: str -> i32

struct String =
    field: str

main () =
    let a = String "a"

    match a
        m => 1

    puts (String.field a)

    ()
